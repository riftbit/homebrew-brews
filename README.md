# riftbit's homebrew-tap

## Getting Started

### installation with Homebrew:

Add brew tap:

```
$ brew tap riftbit/brews
```

Install `commitizen-go` for example:

```
$ brew install commitizen-go
$ sudo commitizen-go install
```
